# Database Connection URL (TiDB/MySQL)
# Example: mysql+pymysql://<user>:<password>@<host>:<port>/<dbname>?ssl={"ca":"/path/to/ca.pem"}
DATABASE_URL=mysql+pymysql://user:pass@host:4000/dbname

# Set to 1 to serve the built frontend from the backend (for monolith deployment)
SERVE_FRONTEND=0
